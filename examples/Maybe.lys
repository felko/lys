module Maybe (Maybe, mapId)

import Bidule.Test

type Maybe<T> = +{ just: T, nothing }

process mapId<A, B>(f: A -o B, mx: ~(Maybe<A>), my: Maybe<B>) {
    match mx { just(x): my.just!(x)
             | nothing: my.nothing! }
}
